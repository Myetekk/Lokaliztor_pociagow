Aby uruchomić program należy w konsoli przejść do folderu, w którym zapisany jest projekt, i użycie następujących komend:

"g++ -o main main.cpp workers/Find_Train_by_Distance.cpp workers/Find_Train_by_GPS.cpp -lstdc++"

"./main"